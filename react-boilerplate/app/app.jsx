// we removed the script tags from the html, so now need to require
var React = require('react');
var ReactDOM = require('react-dom');

ReactDOM.render(
  <h3>React Boilerplate Working!!</h3>,
document.getElementById('app')
);
